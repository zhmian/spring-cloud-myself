spring:
  application:
    name: feign-client
  cloud:
    config:
#      环境
      #      分支
      label: master
#      config 服务端
      uri: http://localhost:9100
      profile: prod

# 监控的管理,开放所有端点
management:
  endpoints:
    web:
      exposure:
        include: "*"